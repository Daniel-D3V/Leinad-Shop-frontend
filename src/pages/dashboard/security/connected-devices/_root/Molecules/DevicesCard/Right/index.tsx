import { ReactNode } from "react"

type DevicesCardRightProps = {
    children: ReactNode
}

export const DevicesCardRight = ({ children }: DevicesCardRightProps) => {
    return (
        <></>
    )
}