import { styled } from "styled-components";

export const CardDetailsTitleStyled = styled.p`
    font-size: 1rem;
    font-weight: 500;
    color: ${props => props.theme.texts.title};
    margin-bottom: 0;
`