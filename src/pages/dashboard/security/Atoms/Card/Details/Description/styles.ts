import { styled } from "styled-components";

export const CardDetailsDescriptionStyled = styled.p`
    font-size: 0.85rem;
    font-weight: 500;
    color: ${props => props.theme.texts.description};
    margin-bottom: 0;
`