import { styled } from "styled-components";

export const Modal2FAHeaderButtonCloseStyled = styled.button`
    color: ${props => props.theme.texts.subtitle};
    background: transparent;
    border: none;
    outline: none;
    font-size: 1.2rem;
`