import { styled } from "styled-components";

export const StatusMessageTitleStyled = styled.p`
    font-size: 1.1rem;
    font-weight: 600;
    color: ${props => props.theme.texts.title};
    margin-bottom: 5px;
`