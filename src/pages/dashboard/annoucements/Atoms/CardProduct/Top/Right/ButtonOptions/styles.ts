import { styled } from "styled-components";

export const CardProductButtonOptionsStyled = styled.button`
    position: relative;
    background: transparent;
    border: none;
    outline: none;
    color: ${props => props.theme.texts.title};
`