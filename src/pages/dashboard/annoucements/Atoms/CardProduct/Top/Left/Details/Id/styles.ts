import { styled } from "styled-components";

export const CardProductDetailsIdStyled = styled.p`
    font-size: 0.7rem;
    font-weight: 500;
    color: ${props => props.theme.texts.description};
    margin-bottom: 0;
`