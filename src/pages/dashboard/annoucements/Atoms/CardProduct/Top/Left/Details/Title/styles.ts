import { styled } from "styled-components";

export const CardProductDetailsTitleStyled = styled.p`
    font-size: 0.95rem;
    font-weight: 500;
    color: ${props => props.theme.texts.title};
    margin-bottom: 3px;
`