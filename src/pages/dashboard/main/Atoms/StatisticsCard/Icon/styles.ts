import { styled } from "styled-components";

export const StatisticsCardIconStyled = styled.div`
    svg {
        font-size: 2.6rem;
        color: ${props => props.theme.texts.subtitle};
        opacity: 0.8;
    }
`