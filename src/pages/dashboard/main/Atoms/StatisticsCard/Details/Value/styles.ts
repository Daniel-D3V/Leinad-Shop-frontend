import { styled } from "styled-components";

export const StatisticsCardDetailsValueStyled = styled.p`
    color: ${props => props.theme.texts.title};
    font-size: 1.4rem;
    font-weight: 700;
    margin-bottom: 0;
`