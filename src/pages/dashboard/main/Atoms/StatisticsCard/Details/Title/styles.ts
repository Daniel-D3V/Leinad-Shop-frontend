import { styled } from "styled-components";

export const StatisticsCardDetailsTitleStyled = styled.p`
    color: ${props => props.theme.texts.subtitle};
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    opacity: 0.6;
    margin-bottom: 0;
`