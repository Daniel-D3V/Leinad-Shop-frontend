import { styled } from "styled-components";

export const TotalSalesCardDetailsValueStyled = styled.p`
    color: ${props => props.theme.texts.title};
    font-size: 1.8rem;
    font-weight: 600;
    margin-bottom: 0;
`