import { InputProps } from "../../Input"
import { InputPasswordInputStyled } from "./styles"

export const InputPasswordInput = (props: InputProps) => {
    return (
        <InputPasswordInputStyled {...props} />
    )
}