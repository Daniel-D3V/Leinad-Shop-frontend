import { styled } from "styled-components";
import { Input } from "../../Input";

export const InputPasswordInputStyled = styled(Input)`
    border: none !important;
    box-shadow: none !important;
    margin-bottom: 0;
    padding-right: 0;
`