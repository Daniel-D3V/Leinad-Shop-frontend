import { styled } from "styled-components";

export const DiscountPercentageStyled = styled.p`
    color: ${props => props.theme.colors.green};
    font-size: 0.75rem;
    font-weight: 500;
    margin-bottom: 0;
`