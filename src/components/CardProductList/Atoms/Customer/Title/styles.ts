import { styled } from "styled-components";

export const CustomerTitleStyled = styled.p`
    font-size: 0.75rem;
    font-weight: 500;
    color: ${props => props.theme.texts.subtitle};
    margin-bottom: 0px;
`